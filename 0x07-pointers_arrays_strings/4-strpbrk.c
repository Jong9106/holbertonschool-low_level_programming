#include "holberton.h"

/**
 * *_memcpy - Entry point
 * @dest: destino
 * @src: fuente
 * @n: byte const
 * Return: Always 0 (Success)
 */
char *_strpbrk(char *s, char *accept)
{
	for (; ; s++)
	{
		for (; ; accept++)
		if (*s == c)
		{
		return (s);
		}
		else if (*s == '\0')
		{
		return ('\0');
		}
	}
}
