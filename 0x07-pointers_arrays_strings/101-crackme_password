#include "holberton.h"

/**
 * *_strchr - Entry point
 * @s: string
 * @c: compare
 * Return: Always 0 (Success)
 */
char *_strchr(char *s, char c)
{
	for (; ; s++)
	{
		if (*s == c)
		{
		return (s);
		}
		else if (*s == '\0')
		{
		return ('\0');
		}
	}
}
